spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hospital_nearby
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  logging:
    level:
      org.flywaydb: DEBUG